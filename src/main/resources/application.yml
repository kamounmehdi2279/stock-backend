spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mystock?createDatabaseIfNotExist=true
    username: root
    password: mehdouch
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true

server:
  port: 8085


